# 继承 gitleaks 内置默认规则，保留默认密钥检测能力
[extend]
useDefault = true

# 需要 allowlist/自定义规则时在此追加：
# [allowlist]
# description = "测试与文档中的示例密钥"
# regexes = [
#   '''EXAMPLE[A-Z0-9_]+''',
# ]
# paths = [
#   "tests/",
#   "docs/",
# ]
